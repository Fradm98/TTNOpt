#!/bin/bash
#PBS -N g_chi_scan_diag
#PBS -l select=1:ncpus=16:mem=350gb
#PBS -l walltime=48:00:00
#PBS -j oe
#PBS -o pbs_g_chi_scan_diag.log

# If your scheduler rejects the combined "select=...:mem=" chunk syntax
# above, split it instead:
#   #PBS -l select=1:ncpus=16
#   #PBS -l mem=350gb

cd $PBS_O_WORKDIR

# --- environment -----------------------------------------------------------
# Same reasoning as chi_scan_diagnostic.pbs -- `conda activate` inside a
# non-interactive PBS job shell is unreliable, so call this env's python by
# absolute path instead.
PYTHON="$HOME/.conda/envs/ttn/bin/python"

# Must match select=1:ncpus=16 above.
export OMP_NUM_THREADS=16
export MKL_NUM_THREADS=16
export OPENBLAS_NUM_THREADS=16

echo "Job started on $(hostname) at $(date)"

# -u: unbuffered, tail -f-able in real time.
"$PYTHON" -u g_chi_scan_diagnostic.py > ../5_Z3/logs/g_chi_scan_diagnostic_run.log 2>&1

echo "Job finished at $(date)"
